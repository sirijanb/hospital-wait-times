@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to your login page.
        // Use "authentication/login" if using Identity UI.
        Navigation.NavigateTo("/login");
    }
}